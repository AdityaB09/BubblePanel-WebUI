# --- Core API (FastAPI) ---
fastapi==0.115.2
uvicorn==0.30.6
pydantic==2.9.2
python-multipart==0.0.9
requests==2.32.3
typing_extensions==4.12.2

# --- Imaging / utils (safe for Render, satisfies cv2 import in smoke_test.py) ---
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.3.0
imageio==2.35.1
scikit-image==0.24.0
tqdm==4.66.5
PyYAML==6.0.2
regex==2024.9.11

# --- Optional OCR (only useful if tesseract binary is present; fine to leave installed) ---
pytesseract==0.3.10

# --- Optional: if you plan to run the encoder/embeddings flow on a machine with GPU/CPU headroom ---
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2
sentence-transformers==2.7.0
transformers==4.44.2
